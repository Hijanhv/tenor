# Rust's output directory
target

# Local settings
.soroban
.stellar

# Node / Next.js
node_modules
.next
out
dist
.turbo
*.tsbuildinfo

# Env & secrets
.env
.env*.local
**/.stellar/identity/*.toml

# OS
.DS_Store

# Keeper runtime state (local last-sync timestamp)
deploy/.keeper_state

# Local-only marketing material (kept out of the public repo)
assets/DEMO_SCRIPT.md
assets/twitter/THREAD.md
